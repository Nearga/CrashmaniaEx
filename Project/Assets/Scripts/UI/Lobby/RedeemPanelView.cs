using UnityEngine;

namespace Crashmania.UI.Lobby
{
    /// <summary>
    /// View for the Redeem tab panel. Currently a placeholder; extend to wire
    /// redeem codes, reward claims, or promotions when the backend supports them.
    /// </summary>
    public sealed class RedeemPanelView : MonoBehaviour
    {
        // Future: add [SerializeField] fields for redeem UI elements here.
    }
}
